import { FoodCategoryPipePipe } from './food-category-pipe.pipe';

describe('FoodCategoryPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FoodCategoryPipePipe();
    expect(pipe).toBeTruthy();
  });
});
