import { MemberByPhonePipe } from './member-by-phone.pipe';

describe('MemberByPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new MemberByPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
