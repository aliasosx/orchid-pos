import { MemberPipePipe } from './member-pipe.pipe';

describe('MemberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MemberPipePipe();
    expect(pipe).toBeTruthy();
  });
});
