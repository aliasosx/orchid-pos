import { ProductCatPipePipe } from './product-cat-pipe.pipe';

describe('ProductCatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductCatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
