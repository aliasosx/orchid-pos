import { PurchaseSearchPipe } from './purchase-search.pipe';

describe('PurchaseSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PurchaseSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
