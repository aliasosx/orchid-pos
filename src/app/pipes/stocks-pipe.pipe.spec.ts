import { StocksPipePipe } from './stocks-pipe.pipe';

describe('StocksPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StocksPipePipe();
    expect(pipe).toBeTruthy();
  });
});
