import { TakeOffByDatePipePipe } from './take-off-by-date-pipe.pipe';

describe('TakeOffByDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TakeOffByDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
