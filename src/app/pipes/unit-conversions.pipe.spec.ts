import { UnitConversionsPipe } from './unit-conversions.pipe';

describe('UnitConversionsPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitConversionsPipe();
    expect(pipe).toBeTruthy();
  });
});
